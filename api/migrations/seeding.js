require("dotenv").config();
const client = require("../app/client");
const faker = require("faker");

const config = {
  MAX_USER: 500,
  MAX_EXPLORATION: 100,
};

const insertUsers = async () => {};
const insertExplorations = async () => {};
